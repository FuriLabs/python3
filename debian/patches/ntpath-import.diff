# ntpath not in python-minimal

--- a/Lib/pathlib.py
+++ b/Lib/pathlib.py
@@ -8,7 +8,6 @@ operating systems.
 import fnmatch
 import functools
 import io
-import ntpath
 import os
 import posixpath
 import re
@@ -349,6 +348,7 @@ class PurePath(object):
         return (self.__class__, self.parts)
 
     def __init__(self, *args):
+        import ntpath
         paths = []
         for arg in args:
             if isinstance(arg, PurePath):
@@ -734,6 +734,7 @@ class PurePath(object):
         """True if the path is absolute (has both a root and, if applicable,
         a drive)."""
         # ntpath.isabs() is defective - see GH-44626 .
+        import ntpath
         if self._flavour is ntpath:
             return bool(self.drive and self.root)
         return self._flavour.isabs(self._raw_path)
@@ -792,6 +793,7 @@ class PureWindowsPath(PurePath):
     On a Windows system, instantiating a PurePath should return this object.
     However, you can also instantiate it directly on any system.
     """
+    import ntpath
     _flavour = ntpath
     __slots__ = ()
 
